<%= render :partial => "manifests/component",
           :formats => [ :html ],
           :locals  => {
             :name              => @name,
             :category          => @category,
             :tag_value         => @tag,
             :instance          => @instance,
             :version           => @versions.last,
             :versions          => @versions,
             :vresource         => @compute_resources.first,
             :compute_resources => @compute_resources
           } %>
